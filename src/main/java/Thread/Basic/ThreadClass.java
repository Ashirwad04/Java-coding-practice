package Thread.Basic;

public class ThreadClass {
    /*
    Constructors:-
            Thread()-Empty constructor
            Thread(Runnabble r)-Taking referance of runnable interface
            Thread(Runnable r, String name)
            Thread(ThreadGroup g, Straing naeme)
            Thread (String name)


     Other method
                   getId()
                   getName()
                   getPriority()
                   getState()
                   getThreadGroup()


                   setName(String name)
                   setProirity(int p)
                   setDaemon(boolean true)



                   isAlice()
                   isDaemon()
                   isIntrupted()




              Instance method
                    interrupt()
                    join()
                    join(long millis)
                    run()
                    start()



              Static method
                     activeCount()
                     currentThread()
                     yield()
                     dupStack()


     */
}
